require 'rubygems'
require 'cmd.rb'


desc "Run the Unit Tests"
task :test do |t|
  files = Find['. -name "*.class"'] 
  #blah = Find['. -name "*.class"'] | "foo"
  puts files.class
  puts "#{files}"
  # puts Find| 2
end

